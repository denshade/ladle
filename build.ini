# Point path at a JDK root without spaces. On first setup, create a junction:
#   mklink /J .jdk "C:\Program Files\Java\jdk-26"
[javac]
path = .jdk
parameters = -encoding UTF-8 -d build/classes

[sources]
paths = src
